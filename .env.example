# Riot Games Developer API Key (Do not commit real keys to version control!)
RIOT_API_KEY=your_riot_api_key_here
DEFAULT_SHARD=na # Shard auto-detected per player session (na, eu, ap, kr, latam, br)
DEFAULT_ROUTING=americas # Global routing cluster (americas, europe, asia)
PORT=8080
